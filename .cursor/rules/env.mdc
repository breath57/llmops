---
description: 前端使用系统默认的node即可，uv使用系统默认的uv即可，后端使用uv搭建python的虚拟环境，对于中间件，例如：Redis或者其他相关的数据库等，直接使用Docker创建启动即可，本地的docker环境是可用的。所有相关依赖的安装，包括docker，请使用阿里的镜像源, docker的使用并不需要sudo，已经集成到linux当前账户中了，请直接使用。大模型请直接使用硅基流动的，其配置信息以及langsimith的配置信息在.config目录下。
alwaysApply: false
---
